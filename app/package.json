{
  "name": "stopslop-macos",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite ui --host 127.0.0.1",
    "build": "vite build ui --outDir ../dist",
    "tauri": "tauri"
  },
  "dependencies": {
    "@tauri-apps/plugin-shell": "^2.0.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0",
    "vite": "^6.0.0"
  }
}
